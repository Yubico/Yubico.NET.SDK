version: "3.8"

services:
  yubi:
    build:
      args:
          USER_ID: 1000
          GROUP_ID: 1000 
          ARTIFACT_DIR: ubuntu-x64
      context: .
      dockerfile: docker/Alpine/Dockerfile
    image: kindtek/yubico-safedb-alpine:0.0.4