<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Yubico.NativeShims" Version="[$(NativeShimsVersion)]" />
  </ItemGroup>

  <Target Name="ValidateNativeShimsVersion" BeforeTargets="CollectPackageReferences">
    <Error Condition="'$(NativeShimsVersion)' == ''"
           Text="NativeShimsVersion is required so the probe restores the package produced by the current build." />
  </Target>
</Project>
