# Transient runtime state from ./view.sh and ./watch.sh
.view-server.pid

# Marp/npx scratch, if it ever lands here
.marp/
node_modules/

# marp --watch output; carries a livereload client, never commit it
deck.dev.html
