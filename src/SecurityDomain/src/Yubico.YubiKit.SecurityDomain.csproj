<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- TargetFrameworks inherited from Directory.Build.props -->
    <RootNamespace>Yubico.YubiKit.SecurityDomain</RootNamespace>
    <AssemblyName>Yubico.YubiKit.SecurityDomain</AssemblyName>

    <!-- Package Metadata -->
    <Description>Security Domain and SCP (Secure Channel Protocol) support for YubiKey devices including SCP03 secure messaging and key management.</Description>
    <PackageTags>yubikey;yubico;security-domain;scp;scp03;secure-channel;globalplatform</PackageTags>

    <!-- Native AOT: opts this SDK library into the shared AOT property block in
         Directory.Build.props/Directory.Build.targets. See docs/NATIVE-AOT.md for the support
         contract. -->
    <IsYubiKitSdkLibrary>true</IsYubiKitSdkLibrary>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Core\src\Yubico.YubiKit.Core.csproj" />
  </ItemGroup>

</Project>
