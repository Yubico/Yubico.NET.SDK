{
  // Dedicated team test devices only. Every device listed here is authorized for
  // DESTRUCTIVE integration tests: application resets, key overwrites, and credential
  // deletion run against it without further confirmation.
  //
  // NEVER add a personal or production key, including one you use for SSH, FIDO2/WebAuthn,
  // PIV certificates, or OATH credentials. Adding a serial here is an authorization to wipe it.
  "YubiKeyTests": {
    "AllowedSerialNumbers": [
      9681620,
      28293700,
      31683481,
      31683268,
      105,
      125,
      100,
      27365355,
      107,
      103,
      20260533
    ],
    // DO NOT set this to true except when testing YubiKey Security Key series devices
    // (FIDO-only editions that report no serial number). Enabling this authorizes ANY
    // connected device with an unreadable serial for destructive integration tests,
    // which can wipe a production key that happens to have serial visibility disabled.
    "AllowUnknownSerials": false
  }
}
